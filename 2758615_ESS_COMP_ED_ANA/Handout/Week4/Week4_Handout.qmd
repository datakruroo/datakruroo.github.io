---
title: "Week4 : Manipulating Data"
author: "Assistant Prof. Siwachoat Srisuttiyakorn"
institute: |
  Department of Educational Research and Psychology <br>
  Faculty of Education Chulalongkorn University
date: today
format:
  revealjs:
    slide-number: c/t
    footer: |
      Assistant Prof. Dr. Siwachoat Srisuttiyakorn <br>
      Department of Educational Research and Psychology <br>
      Faculty of Education Chulalongkorn University
    logo: https://github.com/ssiwacho/picture/blob/main/datakruroo.png?raw=true
    theme: [default, theme.scss]
    css: my_css.css
    scrollable: false
    transition: fade
    background-transition: fade
    highlight-style: github
    embed-resources: false
    code-overflow: scroll
    code-link: true
    code-line-numbers: true
execute:
  echo: false
  freeze: auto
  progress: true
include-in-header:
  - text: |
      <style>
      #title-slide .title {
        font-size: 1.5em;
        color: #C5705D;
      }
      </style>
---

```{r}
library(tidyverse)
library(readxl)
library(here)
```

## library-`here` {.smaller}

-   ใช้สำหรับระบุ relative path ได้อย่างถูกต้องและปลอดภัย

-   อ้างอิงจาก root directory ของโปรเจคโดยอัตโนมัติจาก .Rproj หรือ Git repo

-   ทำให้การอ้างอิงข้อมูลหรือไฟล์ใน project ที่กำลังทำไม่ได้ขึ้นกับ location ของ file ที่กำลังทำงานอยู่

ยกตัวอย่างเช่น

```         
ThesisProject/
├── ThesisProject.Rproj   👈 1. root ที่ here() จะอ้างจาก
├── data/
│   └── my_data.csv       👈 3. data ที่ต้องการเรียก
├── scripts/
│   └── analysis/
│       └── deep_folder/
│           └── script.R  👈 2. สมมุติว่ากำลังเขียน script นี้อยู่
```

การเขียนอ้างอิงไฟล์ `my_data.csv` จาก `script.R` จะเป็นดังนี้

```{r eval = F, echo = T}
library(here)
read_csv(here("data", "my_data.csv"))
```

## Manipulating Data

::::::: columns
:::: {.column width="40%"}
::: {style="font-size:90%;"}
-   Selecting Variables

-   Filtering Cases

-   Transforming Variables

-   Summarise Variables
:::
::::

:::: {.column width="60%"}
<center>

::: {style="font-size:70%;"}
![การจัดกระทำข้อมูลพื้นฐาน (สิวะโชติ ศรีสุทธิยากร, 2564)](images/clipboard-4241219530.png)
:::

</center>
::::
:::::::

## `mytidy2.csv` {.smaller}

```{r echo = T}
mytidy2 <- read_csv(here("Week4/datasets", "mytidy2.csv")) |> 
  rename(student_id = 1)
mytidy2
```

## Selecting variables {.smaller}

คือการคัดเลือกตัวแปรที่ต้องการ ปกติใน R สามารถทำได้หลายวิธีการ ได้แก่ (1) การอ้างอิงด้วย matrix indexing (2) การอ้างอิงด้วย \$ แล้วตามด้วยชื่อคอลัมน์ หรือ (3) การใช้ฟังก์ชัน `select()` ของ `dplyr` package

พิจารณาตัวอย่างต่อไปนี้

```{r echo = T}
mytidy2 |> 
  dplyr::select(infect) |> 
  head(5)
```

```{r echo = T}
mytidy2 %>%
 select(stress.1, stress.2, stress.3,
        stress.4, stress.5, gpax.y2) |> 
 head(5)
```

## Helper functions for Selecting variables {.smaller}

-   `everything()`

-   `last_col()`

-   `starts_with("prefix")`

-   `ends_with("suffix")`

-   `contains("string")`

-   `matches("regex")`

```{r echo = T}
mytidy2 %>%
 select(starts_with("stress"), gpax.y2) |> 
 head(5)
```

## Mutating variables {.smaller}

-   เป็น grammar สำหรับสร้างตัวแปรใหม่จากตัวแปรที่มีอยู่แล้ว โดยใช้ฟังก์ชัน `mutate()` ของ `dplyr` package เทียบเท่ากับการแปลงค่าของตัวแปร

-   รวมคะแนน `stress` ทั้ง 5 ครั้งเป็นตัวแปรใหม่ `stress_total`

```{r echo = T}
mytidy2 %>%
  mutate(stress_total = stress.1 + stress.2 + stress.3 +
           stress.4 + stress.5) |> 
  select(name, starts_with("stress")) |> 
  head(10)
```

## Mutating variables {.smaller}

การแปลงค่าตัวแปรต่อเนื่องเป็นตัวแปรจัดประเภท สามารถทำได้หลายวิธีการ เช่น การใช้ฟังก์ชัน `ifelse()` หรือ `case_when()` ของ `dplyr` package

```{r echo = T}
mytidy2 |> 
  mutate(result = ifelse(gpax.y2 >= 1.5, "pass", "fail")) |> 
  select(name, gpax.y2, result) |>
  head(6)
```

```{r echo = T}
mytidy2 |> 
  mutate(result = case_when(
    gpax.y2 >= 1.5 ~ "pass",
    TRUE ~ "fail"
  )) |>
  select(name, gpax.y2, result) |>
  head(6)
```

## กิจกรรมคำนวณ `engagement score`

-   คำนวณคะแนน engagement ของนักเรียนรายบุคคล โดยใช้ค่าเฉลี่ยของ `stu.engage.XXX`

-   สร้าง visualization เพื่อบรรยายแนวโน้ม engagement ของนักเรียน

-   คำนวณค่าสถิติบรรยายเพื่อบรรยายแนวโน้ม engagement ของนักเรียน

ผลลัพธ์ที่ได้เป็นอย่างไร?

## กิจกรรมต่อมา

ชุดข้อมูล `mytidy2` มีตัวแปรจัดประเภทจำนวน 3 ตัวได้แก่

-   `infect` : ตัวแปรจัดประเภทที่บอกว่ามีการติดเชื้อหรือไม่ (1 = yes, 2 = no)

-   `stu.itcap` : ตัวแปรจัดประเภทที่บอกว่ามีความสามารถในการใช้เทคโนโลยีหรือไม่ (1 = low, 2 = medium, 3 = high)

-   `internet` : สัญญาณ internet ที่บ้าน (1 = noproblem, 2 = problem)

ขอให้เปลี่ยนสถานะของตัวแปรทั้ง 3 ให้เป็น factor ทั้งหมด

## `across()` {.smaller}

`across()` เป็นฟังก์ชันที่ใช้ภายใน `mutate()`, `summarise()`, หรือ `filter()`\
เพื่อ **จัดการหลายคอลัมน์ในคราวเดียว** ได้อย่างง่ายและยืดหยุ่น

-   ลดการเขียน code ซ้ำ ๆ

-   ใช้สั่งงาน/ประมวลผลหลายคอลัมน์พร้อมกันด้วยฟังก์ชันเดียวกัน หรือเงื่อนไขเดียวกัน

```{r eval = F, echo = T}
data |> 
  mutate(across(.cols, .fns))
```

-   `.cols` ใช้กำหนดคอลัมน์ที่ต้องการ สามารถใช้ได้หลายวิธีการ เช่น `everything()`, `starts_with()`, ... , `where(is.numeric)` หรือ `c("col1", "col2")`

-   `.fns` ใช้กำหนดฟังก์ชันที่ต้องการใช้กับคอลัมน์ที่เลือก เช่น `mean`, `sum`, `as.factor`, `as.character`, ... หรือสามารถใช้ `list()` เพื่อระบุหลายฟังก์ชันได้

    -   ฟังก์ชันเดียว สามารถระบุชื่อฟังก์ชันได้เลย เช่น `mean`, `log`, `scale`

    -   ฟังก์ชันที่มีการระบุ argument สามารถใช้ `~` เพื่อระบุฟังก์ชัน เช่น `~ log(.x + 1)`, `~ scale(.x, center = TRUE, scale = TRUE)`

    -   ฟังก์ชันหลายตัว สามารถใช้ `list()` เพื่อระบุหลายฟังก์ชัน เช่น `list(mean, sd)`, `list(~ log(.x + 1), ~ scale(.x))`

## Filtering cases {.smaller}

-   การคัดกรองหน่วยข้อมูล เป็นการเลือกหน่วยข้อมูลตามเงื่อนไขที่กําหนดจากชุดข้อมูล การคัดกรองหน่วยข้อมูลใน R สามารถทําได้หลายวิธีการ

-   การคัดกรองหน่วยข้อมูลมักต้องใช้ logical operator เป็นตัวช่วยในการคัดกรอง

<center>
![](images/clipboard-4214111219.png){width="60%"}
</center>


## Filtering cases {.smaller}

```{r echo = T}
## คัดกรองด้วยเงื่อนไขเดียว
mytidy2 |> 
  filter(infect == 2)
```


## Filtering cases {.smaller}

```{r echo = T}
## คัดกรองด้วยหลายเงื่อนไข "และ"
mytidy2 |> 
  filter(infect == 2, gpax.y2 > 3)
```

## Filtering cases {.smaller}

```{r echo = T}
## คัดกรองด้วยหลายเงื่อนไข "หรือ"
mytidy2 |> 
  filter(infect == 2 | gpax.y2 > 3) 
```

## กิจกรรม

1. นักเรียนที่มีเกรดเฉลี่ยสะสมน้อยกว่า 1.5 หรือมากกว่า 3.0 มีจำนวนกี่คน

2. นักเรียนที่เก่งการใช้เทคโนโลยี (กลุ่มคล่องแคล่วมาก) และมีความเครียดในการเรียนน้อยกว่า 3 คะแนน มีจำนวนกี่คน และในจำนวนนี้มีเกรดเฉลี่ยสะสมเป็นเท่าไหร่

## `arrange()` {.smaller}

- ฟังก์ชัน `arrange()` ใช้สำหรับจัดเรียงข้อมูลตามตัวแปรที่ต้องการ

- จัดเรียงจากมากไปน้อย หรือน้อยไปมากก็ได้

```{r echo = T, eval = F} 
mytidy2 |> 
  select(name, gpax.y2)
  arrange(gpax.y2)
  
mytidy2 |> 
  select(name, gpax.y2) |> 
  arrange(desc(gpax.y2))
```

> นักเรียนที่มีเกรดเฉลี่ยสะสมน้อยกว่า 1.5 หรือมากกว่าเท่ากับ 3.0 และเคยติดเชื้อ
Covid-19 คนที่มีคะแนนความเครียด (stress) สูงและต่ําที่สุดคือใคร

## `summarise()` {.smaller}

ฟังก์ชันสำหรับสร้างสถิติบรรยาย (descriptive statistics) เช่น ค่าเฉลี่ย, ส่วนเบี่ยงเบนมาตรฐาน, ค่าต่ำสุด, ค่าสูงสุด, ... จากชุดข้อมูลต้นฉบับ

- Central tendency: `mean()`, `median()`

- Dispersion: `sd()`, `var()`, `range()`

- Range: `min()`, `max()`, `quantile()`

- Count: `n()`, `n_distinct()`

## `summarise()` {.smaller}

> สร้างตารางสถิติพื้นฐานของความเครียดและ gpax ของกลุ่มนักเรียนที่ไม่เคยติดเชื้อ covid-19

```{r echo = T}
mytidy2 |> 
  filter(infect == 2) |> 
  summarise(
    mean_stress = mean(stress.1, na.rm = TRUE),
    sd_stress = sd(stress.1, na.rm = TRUE),
    min_stress = min(stress.1, na.rm = TRUE),
    max_stress = max(stress.1, na.rm = TRUE),
    mean_gpax = mean(gpax.y2, na.rm = TRUE),
    sd_gpax = sd(gpax.y2, na.rm = TRUE)
  )
```



## `group_by()` {.smaller}

-   ฟังก์ชัน `group_by()` ใช้สำหรับจัดกลุ่มข้อมูลตามตัวแปรที่ต้องการ

-   ใช้ร่วมกับ `summarise()` เพื่อสร้างสถิติบรรยายของแต่ละกลุ่ม

-   ใช้ร่วมกับ `mutate()` เพื่อสร้างตัวแปรใหม่ที่เป็นผลลัพธ์ของการคำนวณในแต่ละกลุ่ม

```{r}
mytidy2 |> 
  group_by(infect) |> 
  summarise(
    mean_stress = mean(stress.1, na.rm = TRUE),
    sd_stress = sd(stress.1, na.rm = TRUE),
    n = n()
  )
```

