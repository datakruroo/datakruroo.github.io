---
title: "รายงานผลการตรวจงาน Tidymodels"
subtitle: "นางสาวดวงฤทัย เด่นจารุกูล (6881012427)"
format: 
  html:
    theme: cosmo
    toc: true
    toc-depth: 3
    number-sections: true
editor: visual
date: today
---

## สรุปผลการตรวจ

::: {.callout-note}
**คะแนนรวม:** 75/100 (เกรด C+)
:::

### ✅ **จุดเด่น:**
- การแบ่งข้อมูลด้วย stratified sampling และการตรวจสอบ distribution ทำได้ดีมาก
- การใช้ tidymodels framework สำหรับ Lasso regression ถูกต้องและเป็นระบบ  
- Cross-validation setup เหมาะสมด้วย 10 folds, 3 repeats
- การ tune penalty parameter มีขั้นตอนชัดเจนและใช้ range ที่เหมาะสม
- Variable importance plot ของ Lasso แสดงได้ดีและตีความถูกต้อง
- การใช้ set.seed() เพื่อ reproducibility ครบถ้วน

### ⚠️ **จุดที่ต้องปรับปรุง:**
- ไม่ได้ใช้ recipe() สำหรับ preprocessing (normalize, dummy variables)
- การสร้างโมเดล Linear regression ใช้ base R แทน tidymodels ทำให้ไม่สอดคล้องกัน
- ขาด predicted vs actual plots สำหรับตรวจสอบคุณภาพการทำนาย
- การอธิบายความแตกต่างระหว่าง Linear และ Lasso regression ยังไม่ลึกพอ
- ไม่ได้อธิบายปรากฏการณ์ที่ test RMSE ต่ำกว่า train RMSE ในโมเดล Linear
- การตีความผลลัพธ์ในบริบทการศึกษายังสามารถขยายได้มากกว่านี้

### 💡 **คำแนะนำ:**
- ศึกษาการใช้ recipe() สำหรับ data preprocessing อย่างเป็นระบบ
- ใช้ tidymodels approach ทั้งสองโมเดลเพื่อความสม่ำเสมอ
- เพิ่ม predicted vs actual scatter plots เพื่อตรวจสอบ model performance
- อธิบายข้อดี-ข้อเสียของแต่ละวิธีให้ละเอียดขึ้น
- วิเคราะห์ผลลัพธ์เชิงลึกมากขึ้นในบริบทการศึกษา
- พิจารณาใช้ cross-validation สำหรับ Linear regression ด้วยเพื่อเปรียบเทียบที่ยุติธรรม

## รายละเอียดคะแนน

### **1. Data Preparation & Splitting (17/20)**

| รายการประเมิน | คะแนนได้ | คะแนนเต็ม | หมายเหตุ |
|:---|:---:|:---:|:---|
| การแบ่งข้อมูล train/test | 5 | 5 | ✅ ใช้ 80/20 stratified sampling พร้อมเหตุผลชัดเจน |
| การตรวจสอบความสอดคล้อง | 5 | 5 | ✅ มี histogram เปรียบเทียบ distribution |
| การจัดการข้อมูล | 2 | 5 | ❌ ไม่ได้ใช้ recipe() สำหรับ preprocessing |
| การใช้ set.seed() | 3 | 3 | ✅ ใช้สำหรับ split และ CV |
| การอธิบายขั้นตอน | 2 | 2 | ✅ อธิบายเหตุผลการเลือก stratified sampling |

### **2. Model Building & Tuning (17/25)**

| รายการประเมิน | คะแนนได้ | คะแนนเต็ม | หมายเหตุ |
|:---|:---:|:---:|:---|
| Recipe creation | 2 | 6 | ❌ ไม่ได้ใช้ recipe() เลย |
| Model specification | 4 | 6 | ⚠️ Lasso ถูกต้อง แต่ Linear ใช้ base R |
| Cross-validation setup | 6 | 6 | ✅ 10 folds, 3 repeats เหมาะสม |
| Lasso penalty tuning | 5 | 5 | ✅  กำหนด grid และ fine-tuning เพื่อหาค่า lambda ที่เหมาะสมได้ |
| Workflow organization | 1 | 2 | ⚠️ ใช้ workflow เฉพาะ Lasso |

### **3. Model Evaluation & Metrics (15/20)**

| รายการประเมิน | คะแนนได้ | คะแนนเต็ม | หมายเหตุ |
|:---|:---:|:---:|:---|
| การเลือกใช้เมตริก | 6 | 6 | ✅ RMSE, R², MAE พร้อมการอธิบาย |
| การประเมินใน train/test | 3 | 6 | ⚠️ Linear manual, Lasso ใช้ last_fit() |
| การตรวจสอบ overfitting | 3 | 5 | ⚠️ สังเกตเห็นได้จาก output แต่ไม่ได้อธิบายอย่างชัดเจน | 
| การนำเสนอผลลัพธ์ | 3 | 3 | ✅ ตารางสรุปชัดเจน |

### **4. Comparison & Visualization (11/15)**

| รายการประเมิน | คะแนนได้ | คะแนนเต็ม | หมายเหตุ |
|:---|:---:|:---:|:---|
| ตารางเปรียบเทียบโมเดล | 5 | 5 | ✅ ตารางเปรียบเทียบครบถ้วน |
| การแสดง tuning process | 4 | 4 | ✅ autoplot() แสดงการ tune ชัดเจน |
| Predicted vs Actual plots | 0 | 4 | ❌ ไม่พบผลการวิเคราะห์ส่วนนี้ |
| คุณภาพ visualization | 2 | 2 | ✅ กราฟที่มีชัดเจน มี label |

### **5. Interpretation & Code Quality (14/20)**

| รายการประเมิน | คะแนนได้ | คะแนนเต็ม | หมายเหตุ |
|:---|:---:|:---:|:---|
| การตีความผลลัพธ์ | 6 | 8 | ✅ ตีความเมตริกถูกต้อง แต่ขาดเชิงลึก |
| การอธิบาย Linear vs Lasso | 2 | 4 | ⚠️ อธิบายเบื้องต้นแต่ไม่ละเอียด |
| คุณภาพโค้ด | 3 | 4 | ⚠️ เป็นระเบียบแต่ผสม base R กับ tidymodels ซึ่งเป็น approach ที่ไม่เหมาะสม |
| สรุปและข้อเสนอแนะ | 3 | 4 | ⚠️ สรุปได้แต่ขาดข้อเสนะแนะเชิงลึก |

## ความเห็นเพิ่มเติม

::: {.panel-tabset}

### เชิงเทคนิค (จาก QMD/Code)
- **จุดเด่น**: การใช้ tidymodels สำหรับ Lasso regression แสดงให้เห็นความเข้าใจในแนวคิด modern R workflow และการใช้ stratified sampling แสดงความเข้าใจในสถิติเบื้องต้นได้ดี
- **จุดปรับปรุง**: การผสมผสาน base R (`lm()`) กับ tidymodels ทำให้เกิดความไม่สม่ำเสมอ ควรใช้ `linear_reg()` จาก parsnip แทน และการไม่ใช้ `recipe()` ทำให้พลาดโอกาสในการ preprocessing ที่เป็นระบบ
- **ข้อสังเกต**: โค้ดส่วนใหญ่เขียนได้ดีและมี comment แต่การคำนวณ metrics สำหรับ linear regression ทำแบบ manual ซึ่งเสี่ยงต่อข้อผิดพลาด

### การนำเสนอ (จาก PDF)
- **จุดเด่น**: รายงานมีโครงสร้างชัดเจนตาม requirement และการใช้ภาษาเหมาะสมกับระดับปริญญาโท มีการนำเสนอผลลัพธ์ด้วยตารางและกราฟที่เข้าใจง่าย
- **จุดปรับปรุง**: การตีความผลลัพธ์ยังไม่เชื่อมโยงกับบริบทการศึกษาอย่างลึกซึ้ง และการอธิบายข้อดี-ข้อเสียของแต่ละวิธียังผิวเผิน ขาดการวิเคราะห์เปรียบเทียบที่ครบถ้วน
- **ข้อสังเกต**: การเขียนรายงานแสดงความพยายามที่ดี แต่ยังขาดการคิดวิเคราะห์เชิงวิพากษ์ในบางประเด็น

:::

## สิ่งที่ควรปรับปรุงในครั้งต่อไป

1. **เรียนรู้การใช้ recipe()** - สำหรับ data preprocessing อย่างเป็นระบบ เช่น `step_normalize()`, `step_dummy()`, `step_zv()` เป็นต้น

2. **ความสม่ำเสมอของ workflow** - ใช้ tidymodels approach ทั้งสองโมเดลเพื่อให้เปรียบเทียบได้อย่างยุติธรรม

3. **การตรวจสอบโมเดลให้ครบถ้วน** - เพิ่ม predicted vs actual plots, residual plots เพื่อตรวจสอบ assumptions

4. **การตีความเชิงลึก** - อธิบายผลลัพธ์ในบริบทการศึกษาให้มากขึ้น เช่น ความหมายของตัวแปรที่สำคัญ, practical significance ในรายงานเราแปะผลลัพธ์มาแต่ขาดการอธิบาย และวิพากษ์ผลลัพธ์ที่ได้ว่าโมเดลที่ train ได้มันดีพอมั้ย การให้ผลการทำนายของโมเดลนี้ โมเดลมีแนวคิดอย่างไรในการทำนาย (ส่วนนี้แปลความได้จาก coefficient ที่เราดึงมา)

5. **การเปรียบเทียบวิธี** - อธิบายข้อดี-ข้อเสีย, เมื่อไหร่ควรใช้วิธีไหน, trade-offs ระหว่าง interpretability กับ performance

6. **Cross-validation ที่สม่ำเสมอ** - ใช้ CV สำหรับทั้งสองโมเดลเพื่อการเปรียบเทียบที่แม่นยำ ใน linear regression เราไปใช้ `lm()` เลยทำ CV บนชุดข้อมูลเดียวกันไม่ได้

7. **การวิเคราะห์ปรากฏการณ์ผิดปกติ** - อธิบายทำไม test RMSE ถึงต่ำกว่า training RMSE ใน linear regression

8. **เพิ่มมิติการวิเคราะห์** - พิจารณา feature selection, model diagnostics, การตรวจสอบ multicollinearity

9. ไม่ควรใช้ base R ร่วมกับ tidymodels เพราะจะทำให้ workflow ไม่สม่ำเสมอและยากต่อการดูแลรักษาโค้ดในระยะยาว รายวิชานี้ไม่ใช่สถิติจะมีประเด็นด้านการพัฒนาของที่เราจะต้องคำนึงถึงด้วยครับ

::: {.callout-tip}

- **โดยรวม:** นิสิตแสดงความเข้าใจพื้นฐานในการใช้ tidymodels ได้ดี โดยเฉพาะการ tune hyperparameters การใช้ cross-validation การเปรียบเทียบโมเดลอย่างเป็นระบบ และวางโครงสร้างรายงานก็มีแนวที่เกือบดูดี แต่ขาด detail สำคัญโดยเฉพาะการเขียนอธิบาย แปลผลการวิเคราะห์ หรือวิพากษ์ผลลัพธ์ที่ได้ (สำคัญในระดับป.เอก) ขาดชื่อตารางและชื่อรูป เวลาอ่านผลแล้วยากที่จะอ้างอิงว่าเรากำลังแปลผลจากตารางไหนรูปไหน บางรูปกินพื้นที่ครึ่งหน้าการที่เราเลือกวางรูปลงไปแสดงว่ามันน่าจะสำคัญในความเห็นของผู้วิเคราะห์ แต่เราไมไ่ด้แปลความหรืออธิบายความหมายอย่างเพียงพอ อันนี้จะเป็นภาระให้ผู้อ่านต้องเข้าใจเอง เป็นการสื่อสารที่เสี่ยงครับ

- **สำหรับเรื่อง recipe()** แม้ว่ายังไม่ได้สอนในคลาส แต่เป็นส่วนสำคัญของ tidymodels workflow ที่ควรเรียนรู้เพื่อการทำงานที่มีประสิทธิภาพมากขึ้น
:::